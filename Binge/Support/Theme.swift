import SwiftUI

extension Color {
    /// The app's dark cinematic ground (#0B0D13) from the approved mockups.
    /// Shares the asset the launch screen uses so the launch → app hand-off
    /// doesn't flash a different shade.
    static let bingeGround = Color("LaunchBackground")
}
